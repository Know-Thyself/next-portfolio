import styles from '../styles/details.module.css'

const Details = ({ project }) => {
  console.log(project)
  return (
    <div className={styles.container}>This is Details page for now</div>
  )
}

export default Details